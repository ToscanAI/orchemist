APPROVE
[MINOR][correctness] Fence character type not checked in parse_sections or _strip_comments_preserving_fences — a tilde-opened fence (~~~) can be incorrectly closed by a backtick fence (```) and vice versa. CommonMark requires matching character types. In parse_sections the fence-open regex captures both backtick and tilde but the close check only compares length, not character. Same pattern in _strip_comments_preserving_fences. Practical impact is very low (GitHub issues almost never mix fence types), but it's a latent correctness bug. Fix: store the fence character (` or ~) alongside the length and require the closing fence to match both.
[MINOR][design] strip_html_comments() (lines 133-174) is dead code — never called anywhere. extract_and_write() calls _strip_comments_preserving_fences() directly at line 332. The function builds intermediate result_lines that are discarded, then delegates to the same private function anyway. Remove it or make extract_and_write use the public API.
[MINOR][compatibility] .orchemist/runs/49f04f62/acceptance_run.md is committed despite .orchemist/runs/ being in .gitignore. This is a pipeline artifact that leaked into the feature branch (likely from git add -f or pre-existing index entry). Should be removed from the commit before merge.
[NITPICK][style] scripts/extract_issue.py has a shebang (#!/usr/bin/env python3) but file mode is 664 (not executable). Either chmod +x or remove the shebang for consistency. The spec shows invocation via `python3 scripts/...` so it works either way.

VERDICT: APPROVE
COMMENT: Code is clean, well-structured, and implements the spec faithfully. All 28 acceptance tests pass. The fence character-type mismatch is a real but low-impact correctness bug (tilde fences are vanishingly rare in GitHub issues). The dead code and leaked pipeline artifact are housekeeping items. No blockers — ready to merge after optional cleanup.
